{
  "name": "motus-ui-workspace",
  "private": true,
  "version": "0.1.0",
  "description": "Workspace for the Motus UI React library and component showcase.",
  "license": "MIT",
  "packageManager": "npm@11.8.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @motus-ui/site",
    "build": "npm run build:package && npm run build:site",
    "build:package": "npm run build --workspace motus-ui",
    "build:site": "npm run build --workspace @motus-ui/site",
    "check": "npm run check --workspace motus-ui && npm run check --workspace @motus-ui/site",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "verify:package": "node scripts/verify-package.mjs"
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
